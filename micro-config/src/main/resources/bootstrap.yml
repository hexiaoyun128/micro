spring:
  application:
    name: micro-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hexiaoyun128/micro-config.git
eureka:
  client:
    service-url:
      defaultZone: http://cloudy:cloudy@${EUREKA_HOST:localhost}:${EUREKA_PORT:8812}/eureka/
server:
  port: 8811